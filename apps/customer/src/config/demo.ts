/**
 * Demo / preview mode flag.
 *
 * Set DEMO_MODE = true to run the app with local fake data — no backend
 * required.  All API calls are short-circuited and store state is pre-seeded
 * with realistic sample data (Indian shops, products, orders, profile).
 *
 * Set to false (or __DEV__ === false) before publishing to the store.
 */
export const DEMO_MODE = true;
